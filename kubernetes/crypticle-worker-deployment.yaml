apiVersion: apps/v1
kind: Deployment
metadata:
  name: crypticle-worker
spec:
  replicas: 1
  selector:
    matchLabels:
      component: crypticle-worker
  template:
    metadata:
      labels:
        component: crypticle-worker
    spec:
      containers:
        -
          name: crypticle-worker
          image: 'socketcluster/crypticle:v1.1.2'
          ports:
            -
              name: crypticle-worker
              containerPort: 8000
          env:
            -
              name: AGC_STATE_SERVER_HOST
              value: agc-state
            -
              name: AGC_INSTANCE_IP
              valueFrom:
                fieldRef:
                  fieldPath: status.podIP
            -
              name: ENV
              value: prod
          readinessProbe:
            httpGet:
              path: /health-check
              port: crypticle-worker
            initialDelaySeconds: 5
            timeoutSeconds: 10
          livenessProbe:
            httpGet:
              path: /health-check
              port: crypticle-worker
            initialDelaySeconds: 15
            timeoutSeconds: 20
          volumeMounts:
            -
              mountPath: /usr/src/app
              name: app-src-volume
      volumes:
        -
          name: app-src-volume
          emptyDir: {}
      initContainers:
        -
          name: app-src-container
          image: ""
          volumeMounts:
            -
              mountPath: /usr/dest
              name: app-src-volume
          command:
            - cp
            - '-a'
            - /usr/src/.
            - /usr/dest/
